{
  "dependencies": {
    "@tailwindcss/vite": "^4.0.13",
    "tailwindcss": "^4.0.13"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "vite",
    "build": "vite build",
    "watch": "npx @tailwindcss/cli -i ./src/input-css/input.css -o ./src/css/style.min.css --minify --watch"
  }
}
